#!/usr/bin/python3
"""amenity module """
from models.base_model import BaseModel


class Amenity(BaseModel):
    """usage Amenity(str)"""

    def __init__(self)
    super().__init__()
    self.name = str()
